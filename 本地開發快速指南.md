# 🚀 本地開發快速指南

## ⚡ 快速啟動

### 方法 1: 使用啟動腳本（推薦）
```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard
.\start-dev.ps1
```

### 方法 2: 直接使用 npm
```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard
npm run dev
```

## 🌐 訪問應用

啟動成功後，在瀏覽器打開：
- **主頁面**: http://localhost:3000
- **API 測試**: http://localhost:3000/api/readiness

## 📝 快速修改指南

### 修改數據
編輯 `app/api/readiness/route.ts`
- 修改 ASI 指數、倒數天數、五元素分數
- 保存後自動重新載入

### 修改樣式
編輯 `styles/globals.css` 或 `tailwind.config.ts`
- 調整顏色、字體、間距等
- 保存後自動重新載入

### 修改組件
編輯 `app/components/` 下的組件檔案
- `MainGauge.tsx` - 主儀表
- `DomainsGrid.tsx` - 五元素卡片
- `DomainRadar.tsx` - 雷達圖
- `FooterStatement.tsx` - 文明宣言

### 修改主頁面
編輯 `app/page.tsx`
- 調整頁面佈局和結構

## 🔄 熱重載

Next.js 開發模式支援自動重新載入：
- ✅ 修改 `.tsx`、`.ts` 檔案 → 自動刷新
- ✅ 修改 `.css` 檔案 → 自動刷新
- ✅ 修改 API 路由 → 自動刷新

**無需手動重啟服務器！**

## 🛠️ 常用命令

```powershell
# 啟動開發服務器
npm run dev

# 構建生產版本（測試）
npm run build

# 運行生產版本（本地測試）
npm run build
npm start

# 代碼檢查
npm run lint

# TypeScript 類型檢查
npx tsc --noEmit
```

## 🐛 問題排查

### 端口被占用
如果 3000 端口被占用，Next.js 會自動使用 3001、3002 等。
查看終端輸出確認實際端口。

### 修改不生效
1. 檢查檔案是否保存
2. 檢查終端是否有錯誤信息
3. 嘗試硬刷新瀏覽器 (Ctrl+F5)

### 服務器無法啟動
```powershell
# 清除緩存並重新安裝
Remove-Item -Recurse -Force node_modules, .next
npm install
npm run dev
```

## 📚 詳細文檔

- **完整開發指南**: `LOCAL-DEVELOPMENT.md`
- **部署指南**: `DEPLOYMENT-GUIDE.md`
- **專案說明**: `README.md`

## 🎯 開發流程

1. **啟動服務器** → `npm run dev`
2. **打開瀏覽器** → http://localhost:3000
3. **修改代碼** → 保存檔案
4. **查看效果** → 自動重新載入
5. **重複迭代** → 持續改進

---

**開始開發吧！** 🚀


