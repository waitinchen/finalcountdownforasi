# 🔧 故障排除指南 - ERR_CONNECTION_REFUSED

## 問題：無法連接到 localhost:3000

### ✅ 解決方案

#### 方法 1: 使用診斷腳本啟動（推薦）

```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard
.\check-and-start.ps1
```

這個腳本會：
- ✅ 檢查 Node.js 和 npm
- ✅ 檢查專案配置
- ✅ 檢查端口占用
- ✅ 顯示實際的啟動信息

#### 方法 2: 手動啟動並查看輸出

```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard
npm run dev
```

**重要**：保持終端窗口打開，查看輸出信息：
- 如果看到 "Ready" 或 "Local: http://localhost:3000"，表示成功
- 如果看到錯誤信息，請記錄下來

#### 方法 3: 檢查並清理後重新啟動

```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard

# 停止所有 Node 進程（如果需要）
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force

# 清理緩存
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# 重新安裝依賴（如果需要）
# Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
# npm install

# 啟動開發服務器
npm run dev
```

## 🔍 常見問題

### 1. 端口被占用

如果 3000 端口被占用，Next.js 會自動使用 3001、3002 等。

**解決方法**：
- 查看終端輸出，確認實際使用的端口
- 或者手動指定端口：
  ```powershell
  $env:PORT=3001; npm run dev
  ```

### 2. 依賴未安裝

**症狀**：啟動時出現模組找不到的錯誤

**解決方法**：
```powershell
npm install
```

### 3. 構建錯誤

**症狀**：啟動時出現 TypeScript 或編譯錯誤

**解決方法**：
```powershell
# 檢查類型錯誤
npx tsc --noEmit

# 檢查代碼問題
npm run lint
```

### 4. 防火牆阻擋

**症狀**：服務器啟動但無法訪問

**解決方法**：
- 檢查 Windows 防火牆設置
- 暫時關閉防火牆測試（不推薦長期使用）

### 5. 多個 Node 進程衝突

**症狀**：端口顯示被占用但無法訪問

**解決方法**：
```powershell
# 查看所有 Node 進程
Get-Process -Name node

# 停止所有 Node 進程（謹慎使用）
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force

# 然後重新啟動
npm run dev
```

## 📋 檢查清單

在啟動開發服務器前，確認：

- [ ] Node.js 已安裝（`node --version`）
- [ ] npm 已安裝（`npm --version`）
- [ ] 在正確的目錄（有 `package.json`）
- [ ] 依賴已安裝（有 `node_modules` 目錄）
- [ ] 沒有其他服務占用 3000 端口
- [ ] 終端窗口保持打開

## 🎯 快速測試

### 測試 Node.js
```powershell
node --version
```

### 測試 npm
```powershell
npm --version
```

### 測試專案構建
```powershell
cd C:\Users\waiti\finalcountdownforasi\asi-countdown-dashboard
npm run build
```

### 測試端口
```powershell
netstat -ano | findstr :3000
```

## 💡 下一步

1. **使用診斷腳本**：`.\check-and-start.ps1`
2. **查看終端輸出**：確認服務器是否成功啟動
3. **檢查實際端口**：如果 3000 被占用，使用終端顯示的端口
4. **如果仍有問題**：記錄終端的完整錯誤信息

---

**需要幫助？** 請提供終端輸出的完整錯誤信息。

